---
title: "Untitled"
author: "JT"
date: "25 juni 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r, fig.width=8, fig.height=4}
library(wordcloud2)
library(dplyr)

test <- as_data_frame(sample(month.name, 100, replace = TRUE)) %>% group_by(value) %>%  summarise(freq = n()) %>% rename(word = value) %>% as.data.frame()
test <- as.data.frame(cbind(state.name,state.area)) %>% rename(word = state.name, freq = state.area) %>% mutate(word = as.character(word), freq = as.integer(freq))

#wordcloud2(test)

#wordcloud2(test, shape = "star")

#wordcloud2(demoFreq, shape = "star")

#letterCloud(demoFreq, word = "ALOTMORE", minRotation = -pi/2, maxRotation = pi/2)

#head(demoFreq)
#figPath = system.file("examples/t.png",package = "wordcloud2")
#wordcloud2(demoFreq, figPath = figPath, size = 1.5,color = "skyblue")

wordcloud2(demoFreq, figPath = "logo_website3.png", size = 1.5, color = "blue")
```

